# Exercício 01 — Verificar status

## UFW
```bash
sudo ufw status verbose
```

## iptables
```bash
# Regras ativas (filter)
sudo iptables -L -n -v

# Mostra em formato “comandos” (bom para auditoria)
sudo iptables -S
```
