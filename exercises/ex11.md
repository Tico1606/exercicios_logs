# Exercício 11 — Rejeitar porta (REJECT) 5432/tcp (PostgreSQL)

## UFW
```bash
sudo ufw reject 5432/tcp
```

## iptables
```bash
# Para TCP, tcp-reset costuma ser o mais “limpo”
sudo iptables -A INPUT -p tcp --dport 5432 -j REJECT --reject-with tcp-reset
```
